from app.models.agent import Agent

class MonitoringAgent(Agent):
    def __init__(self):
        super().__init__()
        # Add any specific initialization for MonitoringAgent

    # Add methods specific to MonitoringAgent
